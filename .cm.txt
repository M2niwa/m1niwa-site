齿轮调整 + 去掉技术栈说明

1. 手机端也显示齿轮
   之前 @media(max-width:768px) 里把 .gear-wrap 直接隐藏了，现在改为缩小显示：
   112px（桌面 168px）、更贴边（left -22px）、透明度降到 0.45，避免在小屏上抢内容。

2. 双端去掉副齿轮
   移除 #gearSub 的 SVG 段，spinGear() 里也不再驱动它 —— 只留主齿轮随转盘旋转。

3. 去掉技术栈面板的说明文字
   删掉 <p class="section-desc">「从深度学习到后端开发，从 Agent 构建到硬件 DIY」，
   首页 skills 面板与 /skills 独立页同步生效（共用 frag-skills 片段）。
